# File parsing
openpyxl>=3.1.0          # read .xlsx files
xlrd>=2.0.1              # read legacy .xls files
markdown>=3.5.0          # convert Markdown to plain text
beautifulsoup4>=4.12.0   # strip HTML tags from rendered Markdown
pandas>=2.0.0            # tabular data helpers

# HTTP server (optional – only needed when running scripts/serve.py)
flask>=3.0.0

# Embedding-based search (optional – install when you need semantic search)
# sentence-transformers>=2.7.0
# numpy>=1.26.0
